# ==========================================================
# Tarot Bot V3 — Environment Template
# ==========================================================
# Copy this file to `.env` and fill in your real values.
# NEVER commit your real `.env` to git — only `.env.example`.
# ==========================================================

# ----- Discord -----
# Get from https://discord.com/developers/applications → Bot → Reset Token
DISCORD_TOKEN=your_discord_bot_token_here

# ----- 9Router (AI gateway) -----
# Set true to route AI calls through 9Router
NINE_ROUTER_ENABLED=true
NINE_ROUTER_BASE_URL=http://localhost:20128/v1
NINE_ROUTER_API_KEY=

# ----- AI model config (via 9Router or direct) -----
# When NINE_ROUTER_ENABLED=true and both keys are set, the bot will
# automatically fall back to direct Gemini API if 9Router is down or
# returns an error. Leave GEMINI_API_KEY blank to disable fallback.
GEMINI_API_KEY=
GEMINI_MODEL=kr/claude-sonnet-4.5
GEMINI_ENABLED=true
GEMINI_MAX_OUTPUT_TOKENS=4000
GEMINI_CONTINUATION_LIMIT=2
GEMINI_API_TIMEOUT=60
GEMINI_TEMPERATURE=0.75
GEMINI_TOP_P=0.9

# ----- Firebase (optional, for cloud sync) -----
FIREBASE_ENABLED=false
FIREBASE_CREDENTIALS_PATH=firebase-credentials.json
FIREBASE_DATABASE_URL=https://your-project.firebaseio.com
FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# ----- Bot behaviour -----
SYNC_SLASH_COMMANDS=true

# id = Indonesian, en = English
DEFAULT_LANGUAGE=id

# simple / deep / gentle / direct
DEFAULT_READING_MODE=deep
# ----- Support / Donation -----
# Ko-fi link (used by /donate command)
DONATE_KOFI_URL=https://ko-fi.com/vv3yy
# Optional PayPal.me URL
DONATE_PAYPAL_URL=
# Optional custom message shown in the donate embed (leave empty for i18n default)
DONATE_MESSAGE=

# ----- Project Metadata (used by /source) -----
# Public source code repository
REPO_URL=https://github.com/vveej/tarot-bot-discord
# SPDX license name shown in /source
LICENSE_NAME=MIT License
# Author/credits name shown in /source
AUTHOR_NAME=vv3yy
